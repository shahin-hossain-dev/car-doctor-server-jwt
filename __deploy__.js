/**
 *
 * Server side deploy
 * ------------------
 * 1. vercel config:
 * 2. If you use cookies in cross site: use cors for your production url
 * 3. set environment variable
 * 4. // vercel deploy comment করে রাখতে হবে।
 *  await client.connect();
 *  await client.db("admin").command({ ping: 1 });
 * 5.
 */
